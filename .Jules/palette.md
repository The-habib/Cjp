## 2026-07-09 - Added missing ARIA labels to compose and feed buttons\n**Learning:** Found an accessibility issue pattern specific to this app's components: icon-only buttons in the feed and composer lacked `aria-label`s, making them unreadable by screen readers.\n**Action:** Always ensure icon-only buttons have descriptive `aria-label` attributes, especially in recurring layout components like feeds and media tools.
