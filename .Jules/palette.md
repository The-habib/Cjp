## 2024-05-24 - Form Label Associations
**Learning:** Proper label associations in forms using `htmlFor` and `id` attributes are critical for screen reader compatibility and overall usability (clicking the label focuses the input).
**Action:** Always verify proper label-to-input associations when working with forms.
